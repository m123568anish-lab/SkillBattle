name: Backend CI

# DISABLED - Use main CI workflow instead
# This workflow is kept for historical purposes
# All backend checks are now in .github/workflows/ci.yml

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - '.github/workflows/backend.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: ⏭️  Backend CI disabled - using main CI workflow
        run: echo "Backend checks run in ci.yml workflow"
